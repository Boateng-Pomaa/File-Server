<!DOCTYPE html>
<html>

<head>
    <title>Lizzyz Business</title>
</head>

<body>
    <h1>Lizzyz Business</h1>
    {{#each files}}
    <p>{{this}}</p>
   <pre> <p><a href="/download/{{this}}" download>Download</a>   <a href="/preview/{{this}}" target="_blank">Preview</a> </p>  </pre>
     <form action="/sendfile" method="POST">
        <input type="hidden" name="file" value="{{this}}">
        <input type="email" name="email" placeholder="Enter email address">
        <button type="submit">Send to Email</button>
    </form>
    {{/each}}

    {{!-- <script>
        function sendToEmail(file) {
            fetch('/sendfile', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify({ file: file })
            })
                .then(response => {
                    if (response.ok) {
                        alert('Email sent successfully!');
                    } else {
                        alert('Failed to send email.');
                    }
                })
                .catch(error => {
                    console.error('Error:', error);
                    alert('An error occurred while sending the email.');
                });
        }
    </script> --}}
</body>

</html>